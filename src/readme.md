Why this structure?

 - `/features` → domain separation (scales well)
 - `/services` → infrastructure (ethers later)
 - `/store` → global state
 - `/hooks` → orchestration layer
 - `/components/ui` → reusable UI primitives
